diff --git a/node_modules/react-leaflet-hotline/dist/esm/canvas/HotlineCanvas.js b/node_modules/react-leaflet-hotline/dist/esm/canvas/HotlineCanvas.js
index 0000000..0000001 100644
--- a/node_modules/react-leaflet-hotline/dist/esm/canvas/HotlineCanvas.js
+++ b/node_modules/react-leaflet-hotline/dist/esm/canvas/HotlineCanvas.js
@@ -13,6 +13,15 @@ export class HotlineCanvas extends L.Canvas {
         ;
         L.Canvas.prototype._destroyContainer.call(this);
     }
+    _redraw() {
+        // A redraw can still be queued (requestAnimFrame) when the layer is
+        // removed: _destroyContainer deletes _ctx, but the pending frame fires
+        // afterwards and Leaflet's _clear crashes on `this._ctx.clearRect`.
+        // Bail out if the context is gone.
+        if (this._ctx === undefined)
+            return;
+        L.Canvas.prototype._redraw.call(this);
+    }
     _update() {
         if (this._map === null)
             return;
diff --git a/node_modules/react-leaflet-hotline/dist/esm/core/HotPolyline.js b/node_modules/react-leaflet-hotline/dist/esm/core/HotPolyline.js
index 0000000..0000002 100644
--- a/node_modules/react-leaflet-hotline/dist/esm/core/HotPolyline.js
+++ b/node_modules/react-leaflet-hotline/dist/esm/core/HotPolyline.js
@@ -4,9 +4,22 @@ import Converter from '../converter';
 import clipSegment from './clipSegment';
 export default class HotPolyline extends L.Polyline {
     constructor(renderer, coords, getLat, getLng, getVal) {
-        const canvas = new HotlineCanvas(renderer);
+        // Forward the renderer's options (notably `pane`) to the L.Canvas so the
+        // colorize canvas can render into a caller-chosen pane instead of always
+        // landing in the default overlayPane.
+        const canvas = new HotlineCanvas(renderer, renderer._options);
         const latlngs = Converter.toLatLngs(coords, getLat, getLng, getVal);
-        super(latlngs, { renderer: canvas, interactive: false });
+        // `smoothFactor` too: Leaflet drops vertices closer than it to the last
+        // kept one, and a categorical line changes color across a pair of
+        // coincident points — which simplification is guaranteed to collapse,
+        // turning every hard edge into a gradient over the surrounding segment.
+        super(latlngs, {
+            renderer: canvas,
+            interactive: false,
+            ...(renderer._options.smoothFactor === undefined
+                ? {}
+                : { smoothFactor: renderer._options.smoothFactor }),
+        });
         this.projectMap = renderer.projectLatLngs;
         this._canvas = canvas;
     }
