// Marble fill colors, shared by the canvas tile renderer (galleryTileRenderrer)
// and the map legend (PictureLegend) so the two can't drift apart.
export const GALLERY_COLOR = '#ff0'; // default / mine / premium marble (yellow)
export const MUTED_COLOR = '#fa4'; // not-mine / free marble (orange)
export const NO_DATA_COLOR = '#888'; // no value for the current mode (grey)
