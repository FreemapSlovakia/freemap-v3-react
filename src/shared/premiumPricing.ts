/**
 * What premium is sold for right now — displayed only. What is actually charged
 * is the amount on the Polar product the API's `POLAR_PREMIUM_*_PRODUCT_ID`
 * names, so both have to be raised together or the app advertises a price
 * checkout doesn't ask.
 */
export const PREMIUM_PRICE_EUR = 15;
